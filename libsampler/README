Directory for sampler module/library

It can be built either as a library ur used as a module. Library is
prefered.

sys/capability.h may require extra package to be installed:
$ sudo aptitude install libcap-dev
