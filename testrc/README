Auto create files from native fragments in ready-made directories using
sigdef_pack.sh

To reverse (i.e. transform files in this directory into editable signal
definitions), use sigdef_unpack.sh

To edit an idividual signal definition use signal-edit.sh -f mysig.sig

All scripts accept -h for help
